{
  "dependencies": {
    "openclaw": "latest"
  },
  "name": "crabfold-agent",
  "private": true,
  "scripts": {
    "start": "openclaw gateway --allow-unconfigured --host 0.0.0.0 --port ${PORT:-8080}"
  }
}